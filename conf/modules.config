/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    // =========================================
    // nf-core modules (tumor only)
    // =========================================

    withName: 'GATK4_COLLECTREADCOUNTS' {
        ext.args = '--format HDF5 --interval-merging-rule OVERLAPPING_ONLY'
        publishDir = [
            path: { "${params.outdir}/counts" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'GATK4_DENOISEREADCOUNTS' {
        publishDir = [
            path: { "${params.outdir}/counts" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    // =========================================
    // Local GATK modules — standardized to 4.6.2.0
    // =========================================

    withName: 'COLLECTALLELICCOUNTS' {
        container = 'docker.io/broadinstitute/gatk:4.6.2.0'
        publishDir = [
            path: { "${params.outdir}/counts" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'MODELSEGMENTS_SOMATIC' {
        container = 'docker.io/broadinstitute/gatk:4.6.2.0'
        publishDir = [
            path: { "${params.outdir}/model_segments" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'CALLCOPYRATIOSEGMENTS' {
        container = 'docker.io/broadinstitute/gatk:4.6.2.0'
        publishDir = [
            path: { "${params.outdir}/called_segments" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'PLOTDENOISEDCOPYRATIOS' {
        container = 'docker.io/broadinstitute/gatk:4.6.2.0'
        ext.args = { "--minimum-contig-length ${params.somatic_min_contig_length}" }
        publishDir = [
            path: { "${params.outdir}/plots" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'PLOTMODELEDSEGMENTS' {
        container = 'docker.io/broadinstitute/gatk:4.6.2.0'
        ext.args = { "--minimum-contig-length ${params.somatic_min_contig_length}" }
        publishDir = [
            path: { "${params.outdir}/plots" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'ANNOTATE_CNV' {
        publishDir = [
            path: { "${params.outdir}/annotation" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
}
